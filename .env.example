# Auth0 credentials — copy to .env.local and fill in your values.
# Find these in the Auth0 Dashboard → Applications → [your app] → Settings.
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-client-id-here
